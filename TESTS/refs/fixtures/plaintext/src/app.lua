-- Doc reference: ../notes/Tester.md describes the on-disk layout.
-- The comment above is rewritten by the plaintext provider (comments = true);
-- the string literal below is a real code line and must be left alone.
local doc = "../notes/Tester.md"
return doc
